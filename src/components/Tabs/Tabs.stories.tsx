import React from 'react';
import Tabs from './Tabs';

export default {
    title: 'Tabs',
};

export const BasicTabs = () => {
   

    return (
        <div className="flex gap-4">
            <Tabs />
        </div>
    );
};

