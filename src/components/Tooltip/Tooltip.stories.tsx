import React from 'react';
import Tooltip from './Tooltip';

export default {
    title: 'Tooltip',
};

export const BasicTooltip = () => {
   

    return (
        <div className="flex gap-4">
            <Tooltip />
        </div>
    );
};

